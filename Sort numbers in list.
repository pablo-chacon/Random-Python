#!/usr/bin env python3

# Simple program to sort given numbers in a given length list.


# Author: __ekarlsson66@gmail.com__
# Date: __2021_06_24__
# Version: __002__


  """Define length of list with integer, define numbers in list integers only. 
Sort numbers in list. Output sorted list or ValueError."""


empty_list = []

	
try:
	times = int(input("List length :"))

	while times >= 0:	
		number = int(input("Enter a number: "))
		empty_list.append(number)
		times = times - 1
		
		if times == 0:			
			print(sorted(empty_list))
			break

except ValueError:
	print(number, "isn't a valid number. Try harder.")	
